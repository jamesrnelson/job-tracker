All Jobs for <strong><%= @company.name %></strong>

<% @jobs.each do |job| %>
<ul><%= link_to job.title, job_path(job) %> at <%= job.company.name %></ul>
<ul><%= job.title %> at <%= job.company.name %> - <%= link_to "Delete", company_job_path(@company, job), method: "delete" %></ul>
<% end %>

<%= link_to "Create a New Job", new_company_job_path(@company) %>
